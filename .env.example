# Copy this file to .env and fill in your real API keys.
# This project will auto-load .env on import via ai_scientist/config.py (python-dotenv).

# OpenAI-compatible endpoint (also used for DeepSeek via OpenAI protocol)
OPENAI_API_BASE=https://api.openai-proxy.org/v1
OPENAI_API_KEY=

# Anthropic Claude (native) endpoint
CLAUDE_API_BASE=https://api.openai-proxy.org/anthropic
ANTHROPIC_API_KEY=

# Gemini via OpenAI-compatible protocol
GEMINI_API_BASE=https://api.openai-proxy.org/google
GEMINI_API_KEY=

# Optional: If you have a dedicated DeepSeek key but still want to use OpenAI protocol
# DEEPSEEK_API_KEY=
